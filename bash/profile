#!/usr/bin/env bash

function _source_bash_files() {

  local file=""

    for file in /etc/profile.d/*; do
      [ -r "$file" ] \
         && . "$file"
    done

  unset file

}

_source_bash_files

unset -f source_bash_files
