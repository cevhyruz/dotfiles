# shellcheck shell=bash
# vi:ft=sh fdm=marker ts=2 sw=2 et
#
# All your overrides, quick-and-dirty snippet and hacks should go here,
# this file will be sourced after all Bash configurations were loaded.

# sample snippet for setting up pyenv, using _::add_to_path() from
#  {{DOTFILES}}/utils/util.bash

#  if _::add_to_path "${HOME}/.pyenv/bin"; then
#    eval "$(pyenv init -)"
#    eval "$(pyenv virtualenv-init -)"
#  fi
